✅ DW1 context loaded. Ready to assist with:
- Bronze → Silver → Gold medallion architecture
- SCD Type 2 dimensions with microsecond precision
- NULL pattern for current versions (not 9999-12-31)
- Profile hash computation (SHA256, exclude _other fields)
- Bridge tables for multi-valued sets
- Naming:  gold. dim_*, _version_sk, effective_start_ts
- dbt macros:  compute_profile_hash, compute_set_hash, validate_enumeration